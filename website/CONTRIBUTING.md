# Workflow

1. Create feature branch
2. Implement feature
3. Test locally
4. Commit
5. Merge

# Branch naming
feature/hero
feature/web3
feature/dashboard
fix/navbar
perf/hero

# commit style
feat(hero): add cinematic hero

fix(wallet): network detection

perf(scene): optimize particles